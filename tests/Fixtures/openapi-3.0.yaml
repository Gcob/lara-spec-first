# Minimal, valid OpenAPI 3.0 document. `paths` is required at this version.
openapi: 3.0.3
info:
  title: Fixture API
  version: 1.0.0
paths:
  /users/{id}:
    get:
      operationId: showUser
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: A user
