# Rejected: two paths differing only in a parameter name address one endpoint.
openapi: 3.0.3
info:
  title: Fixture API
  version: 1.0.0
paths:
  /users/{id}:
    get:
      operationId: showById
      responses:
        '200': { description: A user }
  /users/{slug}:
    get:
      operationId: showBySlug
      responses:
        '200': { description: A user }
