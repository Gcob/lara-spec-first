import DefaultTheme from 'vitepress/theme'

import './custom.css'

// The default theme, with the stylesheet beside this file layered over it. There
// is no component to override and no slot to fill: everything this site changes
// about the theme is CSS, and custom.css says why for each rule.
export default DefaultTheme
